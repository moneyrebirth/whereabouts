app = "your-app-name"
primary_region = "nrt"

[build]

[mounts]
  source = "whereabouts_data"
  destination = "/data"

[http_service]
  internal_port = 5001
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
